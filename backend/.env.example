# 数据库配置
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=slow_query_db

# 通义千问API配置
DASHSCOPE_API_KEY=your_api_key

# 加密密钥配置
# 使用以下命令生成安全密钥：python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=your_256_bit_base64_encoded_key
